# Параллельная оценка числа π (MPI + GSL)

Учебный проект на C++, демонстрирующий ускорение вычислений при переходе от последовательного выполнения к параллельному с использованием **MPI**. Для генерации случайных чисел применяется сторонняя библиотека **GNU Scientific Library (GSL)**.

## О проекте

Программа оценивает значение числа π методом Монте-Карло:
- В единичном квадрате `[0, 1] × [0, 1]` генерируются случайные точки.
- Считается доля точек, попавших в четверть единичного круга (`x² + y² ≤ 1`).
- Приближённое значение вычисляется по формуле: `π ≈ 4 × (попадания / всего точек)`.


## Требования

- C++11+ компилятор (`g++` или `clang++`)
- CMake ≥ 3.10
- Реализация MPI (OpenMPI или MPICH)
- Библиотека [GSL](https://www.gnu.org/software/gsl/) (GNU Scientific Library)
- ОС: Linux / macOS / WSL (инструкция приведена для Ubuntu/Debian)

## Установка зависимостей

```bash
sudo apt update
sudo apt install -y build-essential cmake libopenmpi-dev openmpi-bin
```

## Сборка и запуск

### Создание директории сборки
```bash
mkdir build && cd build
```

### Конфигурация проекта
```bash
cmake .. -DCMAKE_BUILD_TYPE=Release -DCMAKE_BUILD_PARALLEL_LEVEL=4
```

### Сборка
```bash
cmake --build .
```

### Запуск
```bash
mpirun -np 4 ./pi_mpi
```

## Как измерить ускорение?

1. Запустите программу с `-np 1` и запишите значение `Время (с)`.
2. Запустите с `-np 4` (или другим количеством доступных ядер) и запишите новое время.
3. Рассчитайте ускорение по формуле:
   ```
   Speedup = Время_последовательное / Время_параллельное
   ```
Ожидается близкое к линейному ускорение (`~N`), ограниченное только накладными расходами MPI и особенностями работы планировщика ОС.

## Как это работает

| Компонент | Роль в проекте |
|-----------|----------------|
| **MPI** | Распределяет количество точек между процессами, синхронизирует старт замеров (`MPI_Barrier`), собирает результаты (`MPI_Reduce` с `MPI_SUM` и `MPI_MAX`). |
| **GSL** | Используется высококачественный ГПСЧ `gsl_rng_mt19937` (Mersenne Twister). Каждый процесс получает уникальный `seed`, что гарантирует статистически независимые потоки случайных чисел. |
| **Балансировка** | Общее число точек (`15 000 000 000`) делится между процессами поровну, остаток распределяется по одному на первые `N` рангов. |


## Полученные результаты

### 1. Системная конфигурация

| Параметр | Значение |
|----------|----------|
| Процессор | Intel Core i5-13600K (Raptor Lake) |
| Архитектура | Гибридная: 6 P-ядер (Performance) + 8 E-ядер (Efficiency) |
| Логические процессоры | 20 (6×2 гиперпоток на P-ядрах + 8 E-ядер) |
| Кэш-память | L1: 544/704 KiB, L2: 20 MiB, L3: 24 MiB |
| NUMA | 1 узел |
| Частота | 800 МГц – 5.1 ГГц (динамическое масштабирование) |
| ОС | Linux (Ubuntu/Debian) |
| MPI-реализация | OpenMPI |
| Флаг запуска | --bind-to core |

### 2. Методика измерений

- Объём вычислений: 15 000 000 000 точек на каждый запуск
- Количество серий измерений: 3
- Для каждого числа процессов (1–14) вычислялось среднее время выполнения
- Ускорение рассчитывалось относительно среднего времени при 1 процессе: S(p) = T(1) / T(p)
- Эффективность: E(p) = S(p) / p × 100%

### 3. Усреднённые результаты

| Процессы | Время, с (среднее) | Ускорение | Эффективность, % |
|----------|-------------------|-----------|-----------------|
| 1 | 63.067 | 1.000 | 100.0 |
| 2 | 31.558 | 1.998 | 99.9 |
| 3 | 21.138 | 2.984 | 99.5 |
| 4 | 16.026 | 3.935 | 98.4 |
| 5 | 13.367 | 4.718 | 94.4 |
| 6 | 11.196 | 5.633 | 93.9 |
| 7 | 18.010 | 3.502 | 50.0 |
| 8 | 16.201 | 3.893 | 48.7 |
| 9 | 14.299 | 4.411 | 49.0 |
| 10 | 12.929 | 4.877 | 48.8 |
| 11 | 11.999 | 5.256 | 47.8 |
| 12 | 11.433 | 5.517 | 46.0 |
| 13 | 10.128 | 6.228 | 47.9 |
| 14 | 9.535 | 6.615 | 47.3 |

![acceleration](./graphs/graph.png)

### 4. Анализ результатов

#### 4.1. Масштабирование в пределах P-ядер (1–6 процессов)

При запуске от 1 до 6 процессов наблюдается почти идеальное линейное ускорение (93.9–100% эффективности). Это соответствует количеству высокопроизводительных ядер (P-cores) в процессоре i5-13600K. Все процессы выполняются на ядрах с полной поддержкой инструкций, высокой тактовой частотой и выделенным кэшем L2.

#### 4.2. Аномалия при переходе на гибридную нагрузку (7 процессов)

При добавлении 7-го процесса время выполнения возрастает с 11.20 с до 18.01 с, а эффективность падает до 50.0%. Причина заключается в том, что 7-й процесс попадает на энергоэффективное ядро (E-core), которое имеет:
- Более низкую максимальную частоту (~3.8 ГГц против ~5.1 ГГц у P-ядер)
- Упрощённую микроархитектуру с меньшим количеством исполнительных портов
- Отсутствие поддержки гиперпоточности

Флаг --bind-to core привязывает процессы к логическим ядрам последовательно, без учёта различий в производительности между типами ядер.

#### 4.3. Стабилизация при полной загрузке физических ядер (12–14 процессов)

При использовании всех 14 физических ядер (6P + 8E) эффективность стабилизируется на уровне 46–48%. Ускорение достигает 6.6× при 14 процессах. Это указывает на то, что после первоначального дисбаланса планировщик и среда выполнения MPI адаптируются к неоднородной архитектуре.

#### 4.4. Точность вычислений

Во всех запусках оценка числа π находилась в диапазоне 3.14158–3.14161, что соответствует относительной погрешности менее 0.001%. Параллелизация не вносит систематической ошибки благодаря использованию независимых потоков ГПСЧ с уникальными значениями seed для каждого процесса.

### 5. Выводы

1. Алгоритм Монте-Карло с независимыми генераторами случайных чисел демонстрирует высокую степень параллелизма и минимальные накладные расходы на синхронизацию.
2. На гибридных процессорах Intel 12–14-го поколений автоматическое распределение процессов может приводить к значительному снижению производительности при переходе границы между типами ядер.
3. Практический предел ускорения для данной задачи на i5-13600K составляет ~6.6× при использовании всех 14 физических ядер.
